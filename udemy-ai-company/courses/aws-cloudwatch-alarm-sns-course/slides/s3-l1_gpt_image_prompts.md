# GPT-Image2 Prompts: s3-l1

Use the same premium AWS/SRE educational visual style as VID-001 and VID-002 s1-l1. 16:9 slide, clean operations and troubleshooting aesthetic, white and pale blue background, navy, teal, red, and green accents, readable Japanese text generated inside the image, no logos, no watermark, no local text overlay.

## slide_001.png

Create a Japanese course slide. Large title: `運用の終わりまで見る`. Subtitle: `作成、更新、削除までがハンズオン`. Visual: workflow cards `Create`, `Update`, `Delete`, `Troubleshoot` with completion checkmarks.

## slide_002.png

Create a Japanese course slide. Large title: `Stack Update`. Subtitle: `しきいちを変えて変更管理を体験する`. Visual: `AlarmThreshold` changes from `1` to `2`, update-stack arrow, settings verification.

## slide_003.png

Create a Japanese course slide. Large title: `Stack Delete`. Subtitle: `削除まで実行して残りを防ぐ`. Visual: delete-stack to `DELETE_COMPLETE`, Alarm and Topic fade out cleanly.

## slide_004.png

Create a Japanese course slide. Large title: `メールが届かない`. Subtitle: `まずPendingConfirmationを見る`. Visual: subscription status check, `PendingConfirmation` highlighted then `Confirmed`.

## slide_005.png

Create a Japanese course slide. Large title: `Alarmが通知しない`. Subtitle: `ActionsEnabledとAlarmActionsを見る`. Visual: alarm settings panel with highlighted `ActionsEnabled`, `AlarmActions`, and `State change`.

## slide_006.png

Create a Japanese course slide. Large title: `Policyと暗号化`. Subtitle: `publish権限とKMS設計を分けて見る`. Visual: SNS Topic Policy layer and KMS key layer separated, labels `教材範囲` and `実運用範囲`.

## slide_007.png

Create a Japanese course slide. Large title: `実運用IaC`. Subtitle: `CloudFormation教材からCDK/Terraform運用へ`. Visual: bridge from `CloudFormation教材` to `CDK / Terraform`, with `Learning` and `Production`.

## slide_008.png

Create a Japanese course slide. Large title: `完了条件`. Subtitle: `README通りに再現し、最後に消せる`. Visual: final checklist `Create`, `Confirm`, `Test`, `Update`, `Delete`.
