# S3-L2 GPT-Image2 Prompts

## Shared Rules

- Generate each final slide as a complete 16:9 image.
- All visible text must be generated by GPT-Image2 inside the image.
- Do not add local text overlays after generation.
- Use premium AWS/SRE observability visual direction.
- Japanese text should be large, crisp, and minimal.
- Avoid AWS logos, CloudWatch logos, watermarks, random letters, random numbers, and unreadable UI labels.
- Use abstract educational UI, not a pixel-perfect AWS console copy.

## Slide 1

Title: `Application Map`

Message: `つながりを地図で見る`

Visual: application map with two service nodes and a dependency arrow.

## Slide 2

Title: `呼ぶ側`

Message: `Traffic Generatorを見る`

Visual: caller service highlighted, scheduler context, outgoing invocation line.

## Slide 3

Title: `呼ばれる側`

Message: `Sample APIを見る`

Visual: callee sample API service highlighted with latency and error indicators.

## Slide 4

Title: `線を見る`

Message: `関係の上に異常が出る`

Visual: dependency edge with delay and error marker, relationship health.

## Slide 5

Title: `全体から詳細へ`

Message: `地図は迷子防止`

Visual: map overview leading to selected service detail.

## Slide 6

Title: `実運用での見方`

Message: `依存先も候補に入れる`

Visual: broader dependency map with database, queue, and external service abstractions.
