# S2-L1 GPT-Image2 Prompts

## Shared Rules

- Generate each final slide as a complete 16:9 image.
- All visible text must be generated by GPT-Image2 inside the image.
- Do not add local text overlays after generation.
- Use premium AWS/SRE observability visual direction.
- Japanese text should be large, crisp, and minimal.
- Avoid AWS logos, CloudWatch logos, watermarks, random letters, random numbers, and unreadable UI labels.
- Use abstract educational UI, not a pixel-perfect AWS console copy.

## Slide 1

Title: `テンプレートの地図`

Message: `作るものを先に読む`

Visual: CloudFormation template map, resources grouped into discovery, Lambda, schedule, logs, and SLO sections.

## Slide 2

Title: `Discovery`

Message: `Application Signalsの準備`

Visual: Application Signals discovery setup block, service-linked role concept, service detection doorway.

## Slide 3

Title: `2つのLambda`

Message: `呼ぶ側と呼ばれる側`

Visual: traffic generator Lambda calling sample API Lambda, dependency arrow, service relationship highlighted.

## Slide 4

Title: `Instrumentation`

Message: `Layerと環境変数`

Visual: Lambda instrumentation components, OpenTelemetry layer, execution wrapper environment variable, execution role policy.

## Slide 5

Title: `Schedule`

Message: `低頻度で通信を作る`

Visual: EventBridge Scheduler with slow pulse, small safe traffic, no load test signal.

## Slide 6

Title: `SLOは条件付き`

Message: `最初から作らない`

Visual: conditional SLO creation flow, first traffic and metrics, then SLO activation.
