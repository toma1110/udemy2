# S3-L1 GPT-Image2 Prompts

## Shared Rules

- Generate each final slide as a complete 16:9 image.
- All visible text must be generated by GPT-Image2 inside the image.
- Do not add local text overlays after generation.
- Use premium AWS/SRE observability visual direction.
- Japanese text should be large, crisp, and minimal.
- Avoid AWS logos, CloudWatch logos, watermarks, random letters, random numbers, and unreadable UI labels.
- Use abstract educational UI, not a pixel-perfect AWS console copy.

## Slide 1

Title: `Servicesを見る`

Message: `調査の入口を決める`

Visual: Services overview table, one service selected as investigation starting point.

## Slide 2

Title: `サービス名`

Message: `対象を間違えない`

Visual: two service rows, sample app and traffic generator, sample app clearly highlighted.

## Slide 3

Title: `Call volume`

Message: `通信が来ているかを見る`

Visual: call volume line chart, no traffic versus active traffic comparison.

## Slide 4

Title: `Latency`

Message: `遅くなっていないか`

Visual: latency chart with slow scenario marker and investigation arrow.

## Slide 5

Title: `Availability`

Message: `成功率を見る`

Visual: availability percentage panel, drop during error scenario.

## Slide 6

Title: `次へ進む判断`

Message: `一覧から詳細へ`

Visual: flow from service list to application map to service detail.
