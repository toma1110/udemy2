# S1-L1 GPT-Image2 Prompts

## Shared Rules

- Generate each final slide as a complete 16:9 image.
- All visible text must be generated by GPT-Image2 inside the image.
- Do not add local text overlays after generation.
- Use premium AWS/SRE observability visual direction.
- Japanese text should be large, crisp, and minimal.
- Avoid AWS logos, CloudWatch logos, watermarks, random letters, random numbers, and unreadable UI labels.
- Use abstract educational UI, not a pixel-perfect AWS console copy.

## Slide 1

Title: `Application Signalsの入口`

Message: `アプリの状態をサービス単位で見る`

Visual: observability dashboard entrance, one application service highlighted, health signal, telemetry streams converging into service view.

## Slide 2

Title: `4つの基本指標`

Message: `量、速さ、成功、失敗`

Visual: four clean metric tiles for traffic volume, latency, availability, faults/errors, healthy versus unhealthy contrast.

## Slide 3

Title: `Services`

Message: `まず一覧で状態をつかむ`

Visual: abstract service list table, status indicator, latency column, availability column, operator choosing investigation starting point.

## Slide 4

Title: `Application Map`

Message: `依存関係を地図で見る`

Visual: simple application map with two Lambda-like service nodes connected by traffic, one edge showing delay or failure signal.

## Slide 5

Title: `Service detail`

Message: `原因へ近づく詳細画面`

Visual: detailed service timeline with latency, availability, fault, error charts, path toward logs and traces.

## Slide 6

Title: `SLOへつなげる`

Message: `重要な体験を目標にする`

Visual: service health metric connected to an SLO target, clear boundary between hands-on learning and production data.
