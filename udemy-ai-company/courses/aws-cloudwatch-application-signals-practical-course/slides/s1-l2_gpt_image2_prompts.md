# S1-L2 GPT-Image2 Prompts

## Shared Rules

- Generate each final slide as a complete 16:9 image.
- All visible text must be generated by GPT-Image2 inside the image.
- Do not add local text overlays after generation.
- Use premium AWS/SRE observability visual direction.
- Japanese text should be large, crisp, and minimal.
- Avoid AWS logos, CloudWatch logos, watermarks, random letters, random numbers, and unreadable UI labels.
- Use abstract educational UI, not a pixel-perfect AWS console copy.

## Slide 1

Title: `小さく作って見る`

Message: `低頻度通信の学習環境`

Visual: compact learning lab with two Lambda-style blocks, slow periodic signal, safe sandbox boundary.

## Slide 2

Title: `標準構成`

Message: `SchedulerからLambdaへ`

Visual: clear flow from scheduler to traffic generator Lambda to sample app Lambda, no public endpoint icon, private learning flow.

## Slide 3

Title: `4つのシナリオ`

Message: `正常、遅延、エラー、回復`

Visual: four scenario cards, normal, slow, error, recovery, each connected to changing metric lines.

## Slide 4

Title: `料金の安全策`

Message: `低頻度、停止、削除`

Visual: cost safety guardrails, low-frequency dial, stop traffic switch, delete stack checklist.

## Slide 5

Title: `SLOは後から`

Message: `メトリクス確認後に作る`

Visual: two-step process, first service metrics arrive, second SLO becomes active, avoid premature SLO warning.

## Slide 6

Title: `実行ルール`

Message: `作成系は承認後に実行`

Visual: validation path separated from create update delete path, approval gate before AWS resource operations.
